#split (): If we want to split the given target string according to a particular pattern then we should go for split() function.
import re
l = re.split("\.", "www.durgasoft.com")
print(l)
for t in l:
 print(t)
